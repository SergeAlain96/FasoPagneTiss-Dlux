export default function CartPage() {
  return (
    <div className="page">
      <section className="section-head">
        <h1>Panier</h1>
        <span className="gold-line" />
      </section>

      <div className="card">
        <p>Le panier React sera branché à Firestore ensuite.</p>
      </div>
    </div>
  );
}
